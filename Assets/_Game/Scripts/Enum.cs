public enum Direction
{
    Left,
    Right,
    Forward,
    Backward
}
public enum CameraState
{
    Start,
    Finish
}
public enum State
{
    Idle,
    Dance,
    Jump
}
public enum GameState { 
    MainMenu, 
    GamePlay, 
    Finish 
}